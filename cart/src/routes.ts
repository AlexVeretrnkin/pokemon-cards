import { Routes } from "@angular/router";
import { CartRouteComponent } from './lib/cart-route/cart-route.component';

export const CartRoutes: Routes = [
    {
        path: '',
        component: CartRouteComponent
    }
];